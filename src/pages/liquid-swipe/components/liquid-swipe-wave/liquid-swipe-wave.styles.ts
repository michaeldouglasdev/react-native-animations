import styled from 'styled-components/native'
import MaskedView from "@react-native-community/masked-view";

export const LiquidSwipeWaveContainer = styled(MaskedView)`
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
`