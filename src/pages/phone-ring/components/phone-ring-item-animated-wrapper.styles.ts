import { Animated } from 'react-native';
import styled from 'styled-components/native';

export const PhoneRingItemAnimatedWrapperContainer = styled(Animated.View)`
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
`;